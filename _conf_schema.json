{
	"locale": {
		"description": "Language",
		"type": "string",
		"default": "zh_CN",
		"options": [
			"zh_CN",
			"en_US"
		],
		"labels": [
			"Chinese",
			"English"
		],
		"hint": "Select plugin display language"
	},
	"whitelist_enabled": {
		"description": "Enable group whitelist",
		"type": "bool",
		"default": false,
		"hint": "When enabled, only groups in the whitelist can use plugin features"
	},
	"group_whitelist": {
		"description": "Group whitelist",
		"type": "list",
		"default": [],
		"hint": "Enter group IDs, one per line. When whitelist is enabled, only listed groups can use plugin features"
	},
	"welcome_global_enabled": {
		"description": "Enable welcome message (Global)",
		"type": "bool",
		"default": true,
		"hint": "Global master switch. When disabled, welcome messages will be disabled for all groups"
	},
	"welcome_default_enabled": {
		"description": "Enable welcome for new groups by default",
		"type": "bool",
		"default": true,
		"hint": "Whether to enable welcome messages by default when a new group triggers for the first time"
	},
	"default_mute_duration": {
		"description": "Default mute duration (seconds)",
		"type": "int",
		"default": 30,
		"hint": "Default mute duration when not specified"
	},
	"default_welcome_message": {
		"description": "Default welcome message",
		"type": "text",
		"default": "",
		"hint": "Leave empty to use i18n default. Supported variables: {membername} = new member's name"
	},
	"max_recall_count": {
		"description": "Max recall count",
		"type": "int",
		"default": 10,
		"hint": "Maximum messages that can be recalled at once"
	},
	"enable_long_message_merge": {
		"description": "Enable long message merge",
		"type": "bool",
		"default": true,
		"hint": "When enabled, messages longer than threshold will be sent intact as a single-node merged message"
	},
	"long_message_threshold": {
		"description": "Long message threshold (characters)",
		"type": "int",
		"default": 350,
		"hint": "Messages longer than this will be wrapped intact as a merged message without splitting. Default: 350 characters"
	},
	"inspection_settings": {
		"description": "Inspection Module Settings",
		"type": "object",
		"hint": "Configure inspection monitoring and detection rules",
		"items": {
			"inspection_enabled": {
				"description": "Enable Inspection Monitor",
				"type": "bool",
				"default": false,
				"hint": "Global master switch. When enabled, Inspection will automatically detect group messages and apply rules"
			},
			"inspection_group_blacklist": {
				"description": "Inspection group blacklist",
				"type": "list",
				"default": [],
				"hint": "Groups in this list will not be monitored by Inspection. Enter group IDs, one per line"
			},
			"inspection_user_whitelist": {
				"description": "Inspection global user whitelist",
				"type": "list",
				"default": [],
				"hint": "Users in this list will not trigger Inspection detection. Enter QQ numbers, one per line"
			},
			"inspection_rules_group": {
				"description": "Detection Rules Config",
				"type": "object",
				"hint": "Configure detection rules for keyword and message type monitoring",
				"items": {
					"inspection_rules": {
						"description": "Inspection detection rules",
						"type": "template_list",
						"default": [],
						"hint": "Click the Add button to select a rule template. Supports keyword rules and message type rules",
						"templates": {
							"keyword_rule": {
								"name": "Keyword Rule",
								"hint": "Detect text content based on keyword/regex matching",
								"items": {
									"keywords": {
										"description": "Keyword list",
										"type": "list",
										"default": [],
										"hint": "Supports regular expressions, one keyword per line"
									},
									"time_range": {
										"description": "Active time range",
										"type": "string",
										"default": "",
										"hint": "e.g. Mon-Fri 09:00-18:00 or 22:00-02:00, leave empty for all day"
									},
									"groups": {
										"description": "Target group IDs",
										"type": "list",
										"default": [],
										"hint": "Leave empty to apply to all groups"
									},
									"mute_duration": {
										"description": "Mute duration (seconds)",
										"type": "string",
										"default": "60",
										"hint": "0=no mute, -1=no recall no mute. Supports range like 60~300"
									},
									"recall_delay": {
										"description": "Recall delay (seconds)",
										"type": "int",
										"default": 0,
										"hint": "0 means recall immediately"
									},
									"reply_message": {
										"description": "Reply on hit",
										"type": "list",
										"default": [],
										"hint": "Random reply list. Variables: {id}=QQ number, {name}=nickname, {date}=date, {time}=time"
									},
									"ignore_admin": {
										"description": "Ignore admins",
										"type": "bool",
										"default": true,
										"hint": "When enabled, admin messages will not trigger this rule"
									},
									"ignore_owner": {
										"description": "Ignore group owner",
										"type": "bool",
										"default": true,
										"hint": "When enabled, group owner messages will not trigger this rule"
									},
									"rule_user_whitelist": {
										"description": "Rule-level user whitelist",
										"type": "list",
										"default": [],
										"hint": "Users in this list will not trigger this rule. Enter QQ numbers, one per line"
									},
									"rule_user_monitor_list": {
										"description": "Rule-level monitor list",
										"type": "list",
										"default": [],
										"hint": "Leave empty to monitor all members"
									},
									"kick_threshold": {
										"description": "Cumulative kick threshold",
										"type": "int",
										"default": 0,
										"hint": "Kick user when hit count reaches this threshold. 0=disabled"
									},
									"kick_message": {
										"description": "Kick message",
										"type": "list",
										"default": [],
										"hint": "Random message list. Variables: {id}, {name}, {date}, {time}"
									}
								}
							},
							"type_rule": {
								"name": "Message Type Rule",
								"hint": "Detect based on message type (image/voice/video etc.)",
								"items": {
									"msg_types": {
										"description": "Message type list",
										"type": "list",
										"default": [],
										"hint": "Options: text / image / voice / video / file / face / forward / json, one per line"
									},
									"time_range": {
										"description": "Active time range",
										"type": "string",
										"default": "",
										"hint": "e.g. Mon-Fri 09:00-18:00 or 22:00-02:00, leave empty for all day"
									},
									"groups": {
										"description": "Target group IDs",
										"type": "list",
										"default": [],
										"hint": "Leave empty to apply to all groups"
									},
									"mute_duration": {
										"description": "Mute duration (seconds)",
										"type": "string",
										"default": "60",
										"hint": "0=no mute, -1=no recall no mute. Supports range like 60~300"
									},
									"recall_delay": {
										"description": "Recall delay (seconds)",
										"type": "int",
										"default": 0,
										"hint": "0 means recall immediately"
									},
									"reply_message": {
										"description": "Reply on hit",
										"type": "list",
										"default": [],
										"hint": "Random reply list. Variables: {id}=QQ number, {name}=nickname, {date}=date, {time}=time"
									},
									"ignore_admin": {
										"description": "Ignore admins",
										"type": "bool",
										"default": true,
										"hint": "When enabled, admin messages will not trigger this rule"
									},
									"ignore_owner": {
										"description": "Ignore group owner",
										"type": "bool",
										"default": true,
										"hint": "When enabled, group owner messages will not trigger this rule"
									},
									"rule_user_whitelist": {
										"description": "Rule-level user whitelist",
										"type": "list",
										"default": [],
										"hint": "Users in this list will not trigger this rule. Enter QQ numbers, one per line"
									},
									"rule_user_monitor_list": {
										"description": "Rule-level monitor list",
										"type": "list",
										"default": [],
										"hint": "Leave empty to monitor all members"
									},
									"kick_threshold": {
										"description": "Cumulative kick threshold",
										"type": "int",
										"default": 0,
										"hint": "Kick user when hit count reaches this threshold. 0=disabled"
									},
									"kick_message": {
										"description": "Kick message",
										"type": "list",
										"default": [],
										"hint": "Random message list. Variables: {id}, {name}, {date}, {time}"
									}
								}
							}
						}
					}
				}
			}
		}
	}
}
